[graph]
all-features = true

targets = [
    "x86_64-unknown-linux-gnu",
    "aarch64-unknown-linux-gnu",
    "x86_64-apple-darwin",
    "aarch64-apple-darwin",
    "x86_64-pc-windows-msvc",
    "wasm32-unknown-unknown",
]

[advisories]
unmaintained = "all"
yanked = "warn"
ignore = []

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"

deny = []
skip = []

skip-tree = ["rlibphonenumber-fuzz"]

[licenses]
confidence-threshold = 0.8
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MIT",
    "NCSA",
    "Zlib",
    "BSD-3-Clause",                   # argh, subtle, antlr4rust
    "ISC",                            # aws-lc-rs, rustls-webpki, untrusted
    "MIT-0",                          # aws-lc-sys
    "CDLA-Permissive-2.0",            # webpki-root-certs (Mozilla CA certs)
    "Unicode-3.0",
]
private = { ignore = true }

exceptions = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
